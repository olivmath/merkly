import subprocess
import json

def test_add_support_to_merkletreejs_in_merkly():
    ...
    # result_js = subprocess.run(["yarn"])

    # result_js = subprocess.run(["node", "./test/merkletreejs/merkleTreeTest.js"], capture_output=True, text=True)
    # merkle_root_js = json.loads(result_js.stdout)["merkleRoot"]

    # result_py = subprocess.run(["python", "./test/merkletreejs/merkleTreeTest.py"], capture_output=True, text=True)
    # merkle_root_py = json.loads(result_py.stdout)["merkleRoot"]

    # assert merkle_root_js == merkle_root_py, "Merkle Roots não são iguais."
